from enum import Enum


class Infantry(Enum):
    """
    This Infantry Enum is used for the Risk Card, to loop through all the different troop types.
    """
    Infantry = 1
    Cavalry = 2
    Artillery = 3
